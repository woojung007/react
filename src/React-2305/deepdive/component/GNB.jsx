export default function GNB({ children }) {
    return (
        <header
            style={{
                background: 'gold',
            }}
        >
            {children}
        </header>
    );
}
